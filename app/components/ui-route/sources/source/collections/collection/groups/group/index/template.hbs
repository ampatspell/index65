{{app-row value=(concat source.data.name ' / ' collection.data.name ' / ' group.data.identifier)}}

{{#each images.content as |image|}}
  {{app-row value=(concat '#' image.data.identifier)}}
{{else}}
  {{app-row icon='error' value='No images for this group yet'}}
{{/each}}