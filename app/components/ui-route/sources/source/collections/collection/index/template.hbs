{{#each groups.content as |group|}}
  {{app-row icon='collection' value=(concat '#' group.data.identifier) action=(action 'group' group)}}
{{else}}
  {{app-row icon='error' value='No image groups yet'}}
{{/each}}